# Phase 37 Status

Frontend retrieval-depth normalization now matches the established runtime invariant.

Values below `1` are normalized to `1`, and the UI no longer offers an invalid `0` option.

The phase does not change retrieval strategy, provider behavior, or ranking semantics.

Local test execution is not claimed; repository CI remains authoritative.
